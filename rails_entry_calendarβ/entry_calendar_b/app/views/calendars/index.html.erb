<%= month_calendar do |day| %>
    <% d = day.to_s.gsub('-', '')%>
    <% if  Entry.entry_count(d) > 0 %>
      <%= link_to day, calendar_entries_path(d) %>
    <% else %>
      <%= day %>
    <% end %>
    <%= link_to  'entry create', new_calendar_entry_path(d) %>
<% end %>